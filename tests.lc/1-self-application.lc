;; -*- mode: scheme -*-

[(lambda (identity)
   [(lambda (self-apply)

      (self-apply identity))

    (lambda (self-apply:s)
      (self-apply:s self-apply:s))])

 (lambda (identity:i)
   identity:i)]
